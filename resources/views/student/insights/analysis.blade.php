<x-app-layout>
    <x-slot name="header"><div><p class="text-sm uppercase tracking-[0.3em] text-blue-200">Learning Analytics</p><h1 class="mt-2 text-3xl font-semibold text-white">Analisis kelemahan berdasarkan topik</h1></div></x-slot>
    <div class="grid gap-6 xl:grid-cols-[1fr_1fr]">
        <div class="panel p-6"><h2 class="text-xl font-semibold">Mastery per topik</h2><div class="mt-4 space-y-4">@foreach($masteries as $mastery)<div><div class="mb-2 flex items-center justify-between text-sm"><span>{{ $mastery->topic->subject->name }} / {{ $mastery->topic->name }}</span><span>{{ $mastery->mastery_score }}%</span></div><div class="h-3 rounded-full bg-slate-100"><div class="h-3 rounded-full bg-gradient-to-r from-emerald-400 to-blue-500" style="width: {{ min(100, $mastery->mastery_score) }}%"></div></div><p class="mt-2 text-xs text-slate-500">Total latihan: {{ $mastery->total_attempts }} / Salah: {{ $mastery->wrong_attempts }}</p></div>@endforeach</div></div>
        <div class="panel p-6"><h2 class="text-xl font-semibold">Riwayat level</h2><div class="mt-4 space-y-4">@foreach($attempts as $attempt)<div class="rounded-3xl border border-slate-200 p-4"><div class="flex items-center justify-between"><div><p class="font-semibold">{{ $attempt->tryout->title }}</p><p class="text-sm text-slate-500">{{ optional($attempt->submitted_at)->format('d M Y H:i') }}</p></div><div class="text-right"><p class="text-xl font-semibold">{{ $attempt->score }}</p><p class="text-sm text-slate-500">{{ $attempt->detected_level }}</p></div></div></div>@endforeach</div></div>
    </div>
</x-app-layout>
